/*
Given an array of integers sorted in ascending order, how quickly can we determine if a given integer is in the set?
*/

function hasElement(arr, target) {

}

var arr = [1, 2, 3, 4, 5];
console.log(hasElement(arr, 5));